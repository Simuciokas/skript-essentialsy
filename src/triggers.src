# Triggers

# ------------
on login:
        add 1 to {esx::logincount} #if it doesn't exist, it creates {esx::logincount} to be 0, then adds 1 to it
        wait 3 ticks
        {esx::motd} is set:
                sendempty(player, {esx::motd})

on command:
        {esx::blockcommands::%player%} is set
        send(player, "You are currently blocked from executing any commands")
        cancel event
# ------------
