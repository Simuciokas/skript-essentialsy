# Triggers

# ------------
on login:
        set {esx::logincount} to {esx::logincount} + 1
        wait 3 ticks
        if {esx::motd} is set:
                sendempty(player, {esx::motd})

on command:
        {esx::blockcommands::%player%} is set
        send(player, "You are currently blocked from executing any commands")
        cancel event
        
on first join:
        make player execute command "/spawn"
# ------------
